CPA2API Release v7.2.2-s.5

This release merges upstream backend and frontend developments, resolving integration conflicts, and providing compilation fixes.

### Key Changes:

- **Upstream Backend Integration**:
  - Merged upstream `CLIProxyAPI` updates, including `sanitizeDownstreamWebsocketFallbackRequest` fallback logic.
  - Adapted custom Qwen signatures and resolved code conflicts cleanly.

- **Upstream Frontend Integration**:
  - Merged upstream `CPA-Manager` (via subtree `web/`) selective updates.
  - Incorporated localized UI enhancements, Codex quota support, Grok quota support, and usage polling optimizations.

- **TypeScript & Build Fixes**:
  - Resolved TS compilation error in control panel `AuthFileCard.tsx`.
  - Updated compiled management page under `static/management.html`.

- **Full Backward Compatibility**:
  - Maintained full compatibility with existing configurations, loaded Qwen credentials, and usage statistics.
