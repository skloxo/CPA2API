v7.1.45-s.9

Features:
- Add One-Click Update (一键更新) feature to the management control panel.
- Support automatic host `.env` file updating (via bind mount at `/app/.env`) to ensure update persistence across container recreations.
- Add warning notices for Docker environments on frontend.
- Localization support in English, Chinese (Simplified/Traditional), and Russian.
